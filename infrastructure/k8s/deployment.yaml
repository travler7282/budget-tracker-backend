apiVersion: v1
kind: Namespace
metadata:
  name: default
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: budget-tracker-backend
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: budget-tracker-backend
  template:
    metadata:
      labels:
        app: budget-tracker-backend
    spec:
      containers:
        - name: api
          image: docker.io/travler7282/budget-tracker-backend:0.0.1
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: APP_HOST
              value: 0.0.0.0
            - name: APP_PORT
              value: "8000"
            - name: APP_BASE_PATH
              value: /budget-tracker
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: budget-tracker-secrets
                  key: database-url
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: budget-tracker-secrets
                  key: jwt-secret-key
          readinessProbe:
            httpGet:
              path: /api/v1/readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/v1/healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: budget-tracker-backend
  namespace: budget-tracker
spec:
  type: ClusterIP
  selector:
    app: budget-tracker-backend
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: budget-tracker-strip-prefix
  namespace: budget-tracker
spec:
  stripPrefix:
    prefixes:
      - /budget-tracker
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: budget-tracker-backend
  namespace: budget-tracker
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: budget-tracker-budget-tracker-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - api.travler7282.com
      secretName: budget-tracker-secrets
  rules:
    - host: api.travler7282.com
      http:
        paths:
          - path: /budget-tracker
            pathType: Prefix
            backend:
              service:
                name: budget-tracker-backend
                port:
                  name: http
